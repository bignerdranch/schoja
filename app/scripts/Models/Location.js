function location(userId, name, latitude, longitude, lastModified)
{
    this.userId = userId;
    this.name = name;
    this.latitude= latitude;
    this.longitude = longitude;
    this.lastModified = lastModified;
}