function destination(userId, name, latitude, longitude, lastTraveled)
{
    this.userId = userId;
    this.name = name;
    this.latitude= latitude;
    this.longitude = longitude;
    this.lastTraveled = lastTraveled;
}